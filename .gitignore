# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Cargo lock for libraries (keep for binaries)
# Cargo.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
*.profraw
*.profdata
# GeoDB cache binaries – ignore all generated caches…
crates/geodb-core/data/countries+states+cities.json.gz.*.bin
# …but keep the canonical ALL cache in git (needed for WASM build)
!crates/geodb-core/data/countries+states+cities.json.gz.ALL.bin
!crates/geodb-core/data/countries+states+cities.json.gz
!crates/geodb-wasm/dist/*.js
!crates/geodb-wasm/dist/*.wasm
!crates/geodb-wasm/dist/*.html

# Local blog content (not tracked)
blog/

# Local helper scripts (not tracked)
scripts/

# Python cache / bytecode
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Local venvs
.venv/
venv/
.env_py312/
target/
# Python / maturin artifacts
dist/*.tar.gz
geodb-py-wheels/

# WASM demo output (runtime build, not the committed one under crates/geodb-wasm/dist if you decide to keep that checked in)
#crates/geodb-wasm/
# tmp, to be removed after refactor
/crates/geodb-core-before-refactor/
